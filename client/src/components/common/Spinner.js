import React from 'react';
import '../../styling/App.css';

const Spinner = () => {
    return (
        <div id="spinner-container" >
            <div id="spinner"></div>
        </div>
    );
};

export default Spinner;